<table>
		<tr>
			<th>Referência</th>
			<th>Data</th>
		</tr>

		<% @orders.each do |order| %>
			<tr>
				<td><%= order.reference %></td>
				<td><%= order.created_at.strftime("%Y-%m-%d") %></td>
			</tr>			
		<% end %>

</table>